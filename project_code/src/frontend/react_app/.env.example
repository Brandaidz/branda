# Environment variables for Branda Frontend (React App)

# Base URL for the backend API
# Example: VITE_API_BASE_URL=http://localhost:5001/api
VITE_API_BASE_URL=/api

# Feature flag for enabling/disabling the React-based chat interface (LOT 2 requirement)
# Set to "true" to enable the chat feature, otherwise it might be hidden or disabled.
VITE_FEATURE_REACT_CHAT=true

